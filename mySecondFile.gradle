import com.topera.gradle.ShowColor

task showRed(type: ShowColor, group: "colors") {
    println "$logPrefix Configuring task red"
    color 'red'
    doFirst {
        println "$logPrefix Running task red"
    }
}

task showBlue(type: ShowColor, group: "colors") {
    println "$logPrefix Configuring task blue"
    color 'blue'

    doFirst {
        println "$logPrefix Running task blue"
    }
}